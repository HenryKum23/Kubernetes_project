apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatbot
  namespace: my-app
  labels:
    app: chatbot
spec:
  replicas: 1                          # 1 replica is fine for a chatbot service
  selector:
    matchLabels:
      app: chatbot
  template:
    metadata:
      labels:
        app: chatbot
    spec:
      containers:
        - name: chatbot
          image: YOUR_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/eshop-chatbot:19c07cf89081956fb94fc5dbf0d355f03c7d58b4
          ports:
            - containerPort: 4000
          env:
            # Inject the Anthropic API key from the Kubernetes Secret
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: anthropic-secret
                  key: ANTHROPIC_API_KEY
            # Tell chatbot which URL the eshop is running on (for CORS)
            - name: ESHOP_URL
              value: "https://henrykumahconsult.org"    # Replace with your actual domain
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
          # Liveness probe — restart pod if it becomes unhealthy
          livenessProbe:
            httpGet:
              path: /health
              port: 4000
            initialDelaySeconds: 10
            periodSeconds: 30
          # Readiness probe — only send traffic when pod is ready
          readinessProbe:
            httpGet:
              path: /health
              port: 4000
            initialDelaySeconds: 5
            periodSeconds: 10