apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cluster-workernode-autoscaler
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://kubernetes.github.io/autoscaler   # Official autoscaler Helm chart repo
    chart: cluster-autoscaler
    targetRevision: 9.29.0
    helm:
      values: |
        autoDiscovery:
          clusterName: prod-cluster
        awsRegion: us-east-1
        serviceAccount:
          create: false
          name: cluster-autoscaler                     # Uses the service account created by eksctl
        extraArgs:
          balance-similar-node-groups: true
          skip-nodes-with-system-pods: false
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false